# 效果测评

## 目的与边界

效果测评回答三个问题：

1. verifier 能否识别含物理错误的解答；
2. 诊断能否定位到与 GT 相同的答案区域；
3. unified_rules 相对同模型直接检查是否带来净收益。

测评不承担规则生成。任何被查看并用于修改代码、prompt、规则或阈值的样本都会转为开发集或验证集，不能继续作为最终测试集。

参考答案只提供给 Gemini，用于构造错误 GT 和审计正确样本；Qwen verifier 与无规则基线均不读取参考答案。

## Catalog 口径

仓库中有两个不能混用的 catalog：

| Catalog | 规模 | 当前角色 |
|---|---:|---|
| `catalogs/rules_unified_3000.json` | 1123 条规则、189 个 Scenario Cluster | 当前 development 高召回基线，本轮 5+5 和 20+20 评测对象 |
| `catalogs/rules_unified_3000_runtime_backfilled.json` | 4875 条规则、488 个 Scenario Cluster | 历史运行时参考库；完整配置消融已否决 |

两者不是同一规则集的纯 metadata 变体。每次实验必须在 `run_config.json` 中保存 catalog 路径、大小和 SHA-256，结果只能归因于实际运行的文件。

## 数据划分与防过拟合

正式采用三层数据：

| 划分 | 用途 | 是否允许据此修改系统 |
|---|---|---|
| Development | 调试流程、构造合成回归测试 | 允许 |
| Validation | 选择整体方案、门控和阈值 | 允许，但结果不再视为最终测试 |
| Final test | 冻结系统后的最终报告 | 不允许；若据此修改，必须废弃并重抽 |

约束：

- 按归一化题目文本去重，不只按样本 ID 去重。
- 历史评测集、开发集、验证集和规则构造数据必须进入排除列表。
- `extract_holdout_eval_samples.py --exclude` 会在默认排除集上累加，不会替换默认项。
- 正确样本必须经 Gemini 独立审计；`source_reward_acc=True` 不能直接当作正确标签。
- 不允许把样本 ID、特定题型、目标公式或逐题阈值写入运行时代码。
- 修复应表达一般不变量，并使用合成或最小单元测试验证。
- 标签在 Qwen 运行前冻结，不根据 verifier 输出删除或替换不利样本。
- 现有 5+5、20+20 及其消融样本均已被查看，只能作为 development/历史回归，不能再承担 final-test 泛化结论。

## 指标口径

### 题目级

`diagnostics` 非空视为预测存在物理错误：

- TP：错题且产生诊断；
- FP：正确题但产生诊断；
- TN：正确题且无诊断；
- FN：错题但无诊断。

报告 Recall、Precision、F1、Specificity 和 Accuracy。

### 错误定位级

GT 与诊断按答案中的字符区间或段落区域匹配。模型提供的字符偏移必须先和 `evidence.quote` 验证；不一致时由 quote 在原答案中重新定位。

该指标衡量“是否指向同一答案区域”，不等价于独立的语义等价判断。

### 可靠性

API、结构化输出或语义导航失败是 abstention/error，不得按空诊断计为 TN。必须同时报告：

- 有效输出数和 coverage；
- semantic/parse failure 数；
- 全集指标或共同有效子集指标；
- 若比较两个系统，优先使用两边共同有效样本。

## 当前模型与运行配置

- GT/正确性审计：`gemini-3-flash-preview`
- Verifier/无规则基线：`qwen3-30b-a3b-instruct-2507`
- 语义结构化输出：`forced_tool_call`
- 本轮 catalog：`catalogs/rules_unified_3000.json`
- 本轮 catalog SHA-256：`838506bfa01b67cc0038ee05d5832dcaecbc55a260f736b9107bfbfd52612005`
- 本轮 symbolic：关闭
- 每题最多发布 12 条诊断，每段最多 2 条
- `.env` 提供 `OPENAI_API_KEY` 与 `OPENAI_BASE_URL`/`OPENAI_API_BASE`
- 本地环境：conda `physicsverifier`

与既有 20+20/消融结果对应的关键源码指纹为：

- `core/semantic_rule_checker.py`：`5a943b0259b17423cb48434bbf828c1538b054503ced6a365f3be7f5dfceca1d`；
- `core/physics_rule_verifier.py`：`7abea6c6116473eeda5dacbbe7a4e4b80bc1d370569d281c7f8acb7ada1dbf15`；
- `core/unified_semantic_matcher.py`：`dc94b064fe2bd1189fdf3fb85577b3f3c5730d51897662d4ea79147a8a4796d9`。

这版 Checker Prompt 已明确要求先判断适用性、不惩罚替代推导、引用具体矛盾且不确定时返回空；因此下一步不是继续追加同义 Prompt，而是增加结构化字段和确定性双证据发布门。

## 标准流程

### 1. 本地预检

```bash
conda activate physicsverifier

python -m unittest discover -s tests -p 'test_*.py'
python scripts/run_verifier.py --help
```

### 2. 抽取候选

以下路径仅为示例；新实验必须使用新的 seed 和输出目录，并排除所有已使用集合。

```bash
python scripts/extract_holdout_eval_samples.py \
  --input data/combined_language_only.json \
  --exclude data/holdout_pilot_candidates_20260730.json \
  --exclude data/validation_candidates_20_20_20260730.json \
  --output data/eval_candidates.json \
  --report results/eval_run/selection_report.json \
  --wrong-size 80 \
  --correct-size 50 \
  --max-rollouts 240 \
  --seed 20260731
```

### 3. 审计正确样本

```bash
python scripts/audit_correct_eval_candidates.py \
  --input data/eval_candidates.json \
  --output data/eval_confirmed_correct.json \
  --audit-output results/eval_run/precision_candidate_audit.json \
  --target-size 20 \
  --max-scan 50 \
  --seed 20260731 \
  --model gemini-3-flash-preview
```

只有 `verdict=correct` 且错误列表为空的样本可以进入 precision 集；不足目标数量时流程应失败，不降低标准补齐。

### 4. 构造错题 GT 与冻结 20+20

```bash
python scripts/run_physics_eval_pipeline.py \
  --run-kind validation \
  --run-id validation-YYYYMMDD-build \
  --manifest-output experiments/manifests/validation-YYYYMMDD-build.json \
  --input data/eval_candidates.json \
  --recall-input data/eval_candidates.json \
  --precision-input data/eval_confirmed_correct.json \
  --recall-size 20 \
  --precision-size 20 \
  --seed 20260731 \
  --strong-model gemini-3-flash-preview \
  --check-model qwen3-30b-a3b-instruct-2507 \
  --max-errors 3 \
  --max-recall-scan 80 \
  --unified-catalog catalogs/rules_unified_3000.json \
  --no-symbolic-check \
  --semantic-output-adapter forced_tool_call \
  --semantic-json-attempts 3 \
  --run-quality-audit \
  --require-quality-pass \
  --skip-run \
  --skip-error-eval \
  --skip-question-eval \
  --output-dir results/eval_run/frozen
```

必须检查：

- error dataset 和 question dataset 数量达到目标；
- GT 平均错误数和定位率通过质量门槛；
- manifest 的 `status=completed`，模型、seed、catalog、源码和冻结数据集指纹正确。

### 5. 运行 unified_rules

GT 人工复核通过后，使用同一 output directory 运行完整 verifier 与指标。正式入口会自动关闭跨运行 LLM cache、逐题原子 checkpoint，并把单题语义失败保留为 coverage/error 而不是静默 TN：

```bash
python scripts/run_physics_eval_pipeline.py \
  --run-kind validation \
  --run-id validation-YYYYMMDD-run \
  --manifest-output experiments/manifests/validation-YYYYMMDD-run.json \
  --output-dir results/eval_run/frozen \
  --recall-size 20 \
  --precision-size 20 \
  --strong-model gemini-3-flash-preview \
  --check-model qwen3-30b-a3b-instruct-2507 \
  --unified-catalog catalogs/rules_unified_3000.json \
  --no-symbolic-check \
  --semantic-output-adapter forced_tool_call \
  --semantic-json-attempts 3 \
  --max-per-sample 12 \
  --max-per-paragraph 2 \
  --verifier-progress-interval 1 \
  --run-quality-audit \
  --require-quality-pass \
  --skip-build
```

直接调用 `run_verifier.py` 仅用于开发诊断；此时受控重复必须显式加 `--no-llm-cache --checkpoint-every 1 --continue-on-semantic-error`。

### 6. 运行同模型无规则基线

```bash
python scripts/run_llm_checker_baseline.py \
  --input results/eval_run/frozen/question_eval_dataset_20_20.json \
  --model qwen3-30b-a3b-instruct-2507 \
  --out_json results/eval_run/frozen/baseline_results.json \
  --output-adapter forced_tool_call \
  --flush-every 1 \
  --timeout 180 \
  --progress-interval 1 \
  --no-tqdm
```

不得使用普通文本基线的 JSON 解析失败结果计算主指标。`parse_ok=false` 的样本应作为失败单列。

### 7. 指标与产物

主要脚本：

- `scripts/evaluate_question_level_sets.py`
- `scripts/evaluate_physics_eval_sets.py`
- `scripts/audit_eval_set_quality.py`

必须保留：

- 候选抽取报告与排除数量；
- 正确样本审计报告；
- 冻结后的 error/question datasets；
- `run_config.json` 运行时副本，以及提交到 `experiments/manifests/` 的冻结 manifest；
- verifier main output、symbolic audit 和完整 traces；
- baseline 输出及每题 `parse_ok`、重试记录；
- 题目级和错误定位级指标；
- coverage、失败阶段和实际 wall time。

`results/` 通常由 Git 忽略，适合保存本地或服务器原始产物，但不能作为唯一实验记录。正式运行通过 `--manifest-output experiments/manifests/<run-id>.json` 生成可提交清单；清单完成后必须核对 `status`、阶段退出码和全部 SHA-256。

## 20+20 Validation（2026-07-30 至 31）

该集合与历史排除集及 5+5 development pilot 按题目文本不重叠。

本轮 `run_config.json` 已记录模型、seed、参数和 catalog 指纹，但未记录 Git commit 与工作区是否干净。这是已知的实验溯源缺口；后续冻结实验必须补齐，不应只依赖当前 `HEAD` 反推历史运行代码。

数据质量：

- 扫描 197,632 条源样本；
- 排除 2,288 个已使用问题；
- Gemini 从 35 个 reward-positive 候选中确认 20 个正确样本；
- 15/35 实际有错，观察到的标签噪声为 42.9%；
- 20 道错题共 60 个 GT 错误，60/60 可定位。

可靠性：

| 系统 | 有效输出 | Coverage |
|---|---:|---:|
| unified_rules | 39/40 | 97.5% |
| forced-tool Qwen30B baseline | 39/40 | 97.5% |

两边共同有效集合为 38 题：19 错、19 对。

### 题目级

| 系统 | TP | FP | TN | FN | Recall | Precision | F1 | Specificity | Accuracy |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| Qwen30B direct baseline | 18 | 10 | 9 | 1 | 94.7% | 64.3% | 76.6% | 47.4% | 71.1% |
| unified_rules（1123 条粗化库） | 19 | 16 | 3 | 0 | 100.0% | 54.3% | 70.4% | 15.8% | 57.9% |

配对结果：两者都正确 20 题、都错误 9 题、unified_rules 独占正确 2 题、baseline 独占正确 7 题。精确双侧 McNemar/binomial 检验约为 `p=0.18`，样本量不足以声称显著差异。

### 错误定位级

共同有效的错题子集含 19 题、57 个 GT 错误。

| 系统 | 命中 GT | 预测 findings | Recall | Precision | F1 |
|---|---:|---:|---:|---:|---:|
| Qwen30B direct baseline | 16/57 | 27 | 28.1% | 59.3% | 38.1% |
| unified_rules（1123 条粗化库） | 22/57 | 44 | 38.6% | 50.0% | 43.6% |

规则链提高了错误定位 Recall 和 F1，但题目级 FP 明显增加。19 个正确样本中有 16 个被规则链标错。

### 性能与失败

- unified_rules 顺序运行约 34–35 分钟，约 50–54 秒/题；
- forced-tool baseline 为 5 分 16 秒，约 7.9 秒/题；
- 规则链约慢 6.5 倍；
- unified_rules 有 1 个持久 `topic_shortlist` 结构失败；
- forced-tool baseline 有 1 个三次重试后仍无法解析的工具参数；
- 普通文本 baseline 曾有 8/40 JSON 解析失败，因此不进入正式对照。

## Catalog 配置消融（2026-07-31）

保持冻结数据、Qwen30B、prompt、forced-tool adapter、三次结构化尝试、关闭 symbolic 和诊断上限不变，只把 1123 条粗化库替换为 4875 条 runtime-backfilled 库。

该比较同时改变规则集合、Topic 覆盖、Cluster 数和 runtime metadata，因此只能回答“哪套完整 catalog 配置更好”，不能解释成纯 metadata 因果效应。

### 1123 与 4875 共同有效

两套规则系统各有 1 个 `topic_shortlist` 失败，且失败 ID 不同。共同有效集合为 38 题：20 错、18 对。

| 系统 | TP | FP | TN | FN | Recall | Precision | F1 | Specificity | Accuracy |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1123 条粗化库 | 20 | 15 | 3 | 0 | 100.0% | 57.1% | 72.7% | 16.7% | 60.5% |
| 4875 条 runtime-backfilled 库 | 15 | 12 | 6 | 5 | 75.0% | 55.6% | 63.8% | 33.3% | 55.3% |

配对结果：两者都正确 16 题、都错误 10 题、1123 独占正确 7 题、4875 独占正确 5 题；精确双侧检验 `p=0.774`，样本量不足以声称显著差异。

共同有效的 20 道错题含 60 个 GT 错误：

| 系统 | 命中 GT | Findings | Recall | Precision | F1 |
|---|---:|---:|---:|---:|---:|
| 1123 条粗化库 | 23/60 | 45 | 38.3% | 51.1% | 43.8% |
| 4875 条 runtime-backfilled 库 | 12/60 | 38 | 20.0% | 31.6% | 24.5% |

4875 配置减少了 3 个正确题 FP，但同时增加 5 个错题 FN，错误定位 Recall 和 Precision 均下降。

### 与 direct baseline 的三方对照

三系统共同有效集合为 37 题：19 错、18 对。

| 系统 | TP | FP | TN | FN | Recall | Precision | F1 | Specificity | Accuracy |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 1123 条粗化库 | 19 | 15 | 3 | 0 | 100.0% | 55.9% | 71.7% | 16.7% | 59.5% |
| 4875 条 runtime-backfilled 库 | 15 | 12 | 6 | 4 | 78.9% | 55.6% | 65.2% | 33.3% | 56.8% |
| Qwen30B direct baseline | 18 | 9 | 9 | 1 | 94.7% | 66.7% | 78.3% | 50.0% | 73.0% |

共同 19 道错题、57 个 GT 错误上的错误定位 F1 分别为：1123 配置 43.6%、4875 配置 25.3%、direct baseline 38.1%。

4875 顺序运行用时 38 分 41 秒，约 58.0 秒/题；仍明显慢于 5 分 16 秒的 direct baseline。

### Metadata-only 受控组

为隔离 metadata 作用，另行基于 1123 条库构造了受控 catalog：

- 规则数仍为 1123、Topic-with-rules 为 76、Cluster 为 189；
- Rule ID、标题、触发条件、检查逻辑、symbolic hint 和 Cluster 归属的投影哈希完全相同；
- 只补齐 topic routing、match features、preconditions、negative conditions、evidence requirements 等 runtime metadata；
- 构造过程不读取 validation 样本，也不调用模型。

服务恢复后通过 3/3 稳定性门禁，剩余样本以最多 3 题的可持久化小批次续跑，最终得到 40/40 有效输出。小批次只改变执行调度，模型、prompt、catalog 和参数不变。原始组有 1 题无效，因此配对共同有效集为 39 题：20 错、19 对。

| 系统 | TP | FP | TN | FN | Recall | Precision | F1 | Specificity | Accuracy |
|---|---:|---:|---:|---:|---:|---:|---:|---:|---:|
| 原始 1123 | 20 | 16 | 3 | 0 | 100.0% | 55.6% | 71.4% | 15.8% | 59.0% |
| 1123 + runtime metadata | 9 | 8 | 11 | 11 | 45.0% | 52.9% | 48.6% | 57.9% | 51.3% |

配对结果为两者都正确 11 题、都错误 7 题、原始组独占正确 12 题、metadata 组独占正确 9 题，精确双侧 `p=0.664`。在 20 道错题、60 个 GT 错误上，错误定位 F1 从 43.8% 降至 20.3%。

正确题 FP 由 16/19 降至 8/19：9 题被修复、7 题持续误报、1 题新增误报，配对 `p=0.0215`。但 metadata 同时使 11/20 错题从命中变为漏检；其中 4 题在 Rule 阶段没有检索到规则，7 题检索到 1–2 条规则但未发布诊断。

误报根因为两层：原始组 22 条 FP 诊断在 semantic publish gate 没有可核验的前提、违规特征或证据要求，metadata 组剩余 17 条 FP 虽有 grounding，且都通过了下游 quote/关键词/符号重合检查，但 semantic gate 的题目侧前提与答案侧违规证据仍为空。通用符号重合只能证明表面相关，不能证明物理违规；至少 3 条解释还明确承认被引推导正确后仍发布错误。因此剩余 FP 的主因是 Checker 在弱相关证据上臆造违规并缺少自一致性门，而非单纯 Topic 导航错误。

metadata 回填也不是人工核验的物理谓词：1123 条中 524 条复用参考字段，599 条从规则文本机械生成；609 条 generalized 规则的 runtime `support.count` 均为 0，所有规则默认 `publishable=true`。这些字段会扩张 SRD 并改变 Checker 行为，足以淘汰该配置，但不能把单次运行差值精确解释为高质量 metadata 的纯因果效应。

## 当前结论

- 1123 配置仍保留较强的错误召回和定位信号，但 FP 高；
- 4875 完整配置降低部分 FP，却显著损失错误召回与定位质量；
- 两套规则配置的题目级效果和运行成本都不优于 direct baseline；
- metadata-only 受控组证明当前回填方案会降低 FP，但导致更严重的错题漏检，不可直接采用；
- 三套现有完整配置都不应直接扩大为服务器 final-test 实验。

下一步保留 1123 的高召回路径，仅在独立 development/合成样本上重新设计通用 precision 层：metadata 只作为排序和证据特征，不作为粗暴拒绝条件；发布诊断必须分别从题目与答案中给出可定位的适用性和违规证据，并通过自一致性检查。先做 Checker-only 受控回放，再做 30+30 三次端到端 development；通过后冻结并运行全新 50+50 一次性 validation，最后才进入服务器 final test。预注册门槛见[项目进展](项目进展.md)。

## 复现状态

当前应表述为“本地原始产物完整、可复算，仓库级复现尚未闭环”：

- 原始 20+20 `run_config.json` 记录了模型、seed、参数和 catalog SHA，但未记录 Git commit 与 dirty 状态；
- 4875 消融记录了 `git_head=523071b…`、dirty 状态和关键源码 SHA；metadata-only 记录了数据、catalog 与输出 SHA，但没有完整源码状态；
- `data/` 约 3.1 GB、`results/` 约 693 MB，均被 Git 忽略，不能依赖 GitHub 自动同步到服务器；
- 后续冻结实验必须使用受版本控制的 manifest，并以 `rsync`、对象存储或等价通道传输非 Git 产物后校验 SHA-256。

## 本地与服务器运行

本地小规模：

```bash
conda activate physicsverifier
python -m unittest discover -s tests -p 'test_*.py'
```

服务器大规模运行前：

1. 在实验室 GitHub 同步目录确认 commit：`git rev-parse HEAD`；
2. 检查工作区和数据文件：`git status --short`；
3. 激活同名 conda 环境；
4. 使用 tmux，并把 stdout/stderr 保存到实验目录；
5. 在运行记录中写入 commit、catalog SHA-256、模型、seed、命令和开始时间。

```bash
tmux new -s physicsverifier_eval
conda activate physicsverifier

python scripts/run_verifier.py ... 2>&1 | tee results/eval_run/verifier.log

# Ctrl-b d 离开
tmux attach -t physicsverifier_eval
```

服务器只用于冻结配置的大规模运行，不在服务器结果出来后直接覆盖本地规则库或修改 final-test 标签。
