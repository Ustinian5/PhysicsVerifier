# 项目进展

更新日期：2026-08-08

## 当前阶段

项目已完成规则库构建、语义链路接入、独立 validation、两轮受控消融、P0 实验复现门禁、**P1 无语义损失的结构修复**和 P2 双证据 precision gate 工程收口。P3 的固定抽样、Gemini GT、target-binding 回放和三臂三重复评估入口已实现；最终协议单规则 Gemini 与三臂各一次 Qwen30B development smoke 已闭环。正式 300 例与九格回放尚未运行，因此没有 P3 效果结论；服务器 final test 也未开始。

确定结论如下：

- 1123 条规则库保留为 development 高召回基线；4875 配置和 1123+metadata 硬门控均已被效果实验否决。
- 当前主要瓶颈不是规则数量，而是 Rule/Checker 发布门没有可机检地证明“题目中前提成立”且“答案中确有违规”。
- 现有 Checker Prompt 已包含适用性、替代推导和具体矛盾要求；P2 已将其升级为可机检的双证据 schema 和确定性发布门，不再依赖追加同义 Prompt。
- P3 主门禁明确估计“目标规则已提供时的 Checker + release gate 性能”，不把显式 target binding 冒充 semantic retrieval；secondary retrieval 当前仅作 trace schema audit，待执行 sidecar 落地后才能讨论 production 外部效度。
- 1139 增量候选已被 manifest v2、来源、generalized 双向差分和变更预算门禁确定性拒绝，不能晋升。
- 本地原始产物完整；新评测入口已经能自动生成代码、数据、catalog、Prompt、参数和输出的仓库级冻结清单。历史 20+20 仍保留原有溯源缺口。

## Catalog 状态

| Catalog | 规模 | 生命周期 | 当前决策 |
|---|---:|---|---|
| `catalogs/rules_unified_3000.json` | 1123 Rule / 189 Cluster | development baseline | 保留，作为下一轮对照组 |
| `catalogs/rules_unified_3000_runtime_backfilled.json` | 4875 Rule / 488 Cluster | historical reference | 效果消融已否决，不作为默认库 |
| `results/unified_rules_incremental_combined8_v2/...` | 1139 Rule / 191 Cluster | unpublished candidate | 审计未通过，禁止晋升 |
| `results/eval_ablation_1123_metadata_20_20_20260731/...` | 1123 Rule / 189 Cluster | ablation artifact | 硬门控损失召回，禁止晋升 |

命令行入口当前没有隐式的 unified catalog 默认值。所有实验必须显式传入 catalog；“当前运行库”不再作为版本概念。

## 已完成工作

### 规则库与链路

- 4361 条候选经多样本概括得到 609 条 generalized 规则，与 514 条基线规则组成 1123 条库。
- 完成 `Domain → Topic → Scenario Cluster → Rule` 目录构建和结构验证；1123 条 Rule ID 唯一、引用合法且全部可达。
- 完成 API 语义导航、结构化输出重试、有界恢复、完整 trace 和增量构建基础设施。
- P2 回归已经通过；合并 P3 与响应身份门禁后已在 conda `physicsverifier` 环境完成全量回归，368/368 通过。

### 效果实验

- 20+20 validation：Gemini 3 Flash 构造/审计 GT，Qwen30B 同时作为 verifier 和 direct baseline。
- 共同有效 38 题上，1123 规则链题目级 F1 为 70.4%，direct baseline 为 76.6%；错误定位 F1 分别为 43.6% 和 38.1%。规则链召回更高，但 19 道正确题误报 16 道，且约慢 6.5 倍。
- 1123/4875 完整配置消融：4875 将题目级 F1 从 72.7% 降至 63.8%，错误定位 F1 从 43.8% 降至 24.5%。
- metadata-only 受控消融：FP 从 16 降至 8，但 TP 从 20 降至 9；题目级 F1 从 71.4% 降至 48.6%。当前 metadata 硬拒绝方案不可采用。

详细口径与共同有效子集见[效果测评](效果测评.md)。

### P2 实现状态

- Checker 已支持 `legacy`、`dual_evidence` 和 `dual_evidence_consistency` 三种模式；`run_verifier.py` 与完整测评入口可显式透传模式和 Checker JSON 尝试次数。
- 双证据模式将适用性证据限定为 `question/context`，将违规证据限定为 `prediction`。模型偏移只有在原文切片与 quote 严格相等时才接受；否则仅允许唯一精确匹配修复，多重匹配或无匹配直接失败关闭。
- `dual_evidence_consistency` 在双证据上再要求自一致性状态为 `confirmed_violation`；自我修正、等价/替代解法或不确定均不发布。
- Checker 置信门限固定为 `0.8`，对齐历史 Prompt 的 80% 保守发布要求，且在 P2 效果测试前固定；该值不是根据已查看测试样本调得。
- Checker 传输、解析或 schema 失败会保存 `checker_status`/`checker_failure_count` 和完整 trace，评测器将其单列为 `checker_failure` 并纳入 coverage，不计入 TN/FN 或错误召回分母。
- 非 `legacy` 模式下，bottom-up experience-code 结果仅保留在审计产物，不进入主诊断；两个 evaluator 也不会将该 audit fail 当作预测。
- 已新增 `scripts/run_checker_replay.py`：先以 `--prepare-manifest-only` 绑定 dataset、语义 retrieval trace、catalog、有序 ID 与 SHA-256，然后每次只运行一个 Checker 模式。三臂共享的是冻结 retrieval；由于 Checker prompt/schema 和候选诊断会随模式变化，它们是 **Checker + release gate system arms**，不是对同一份 shared candidates 做 gate-only 投影。
- 回放同时绑定 frozen manifest、Git/source tree、conda runtime/package set、API endpoint 和传输参数；每个非 transport 响应必须记录供应商 `actual_model` 与非空、格内唯一的 `response_id`，实际模型必须精确匹配配置。validation/final 强制 clean worktree 且禁止 Checker cache。raw-response trace 不保存 prompt，resume 会校验 sidecar/trace 前缀并审计 orphan trace。
- `scripts/build_checker_common_valid.py` 已实现三臂配置一致性、typed ID、输入/运行身份、report/result、实际 LLM trace 和逐题终态校验，并冻结均不可覆盖的共同有效集及 manifest。共同有效集用于配对指标；原始 report 的全量 coverage/failure_counts 与共同有效 manifest 的 failure_by_stage 仍单独报告。

当前统一状态：**P2 工程收口已通过；P3 工程链路已落地，下一步运行正式规则级合成门禁；尚无新效果结论**。所有 smoke 只验证真实端点、严格 schema 和产物绑定链路，不作为 Precision、Recall、FPR 或模式优劣证据。

### P3 实现状态

- `catalogs/rules_unified_3000.json` 实际含 1123 条唯一规则，当前指纹为 `838506bfa01b67cc0038ee05d5832dcaecbc55a260f736b9107bfbfd52612005`。固定 seed `P3_RULE_SAMPLE_V1_20260808` 从 catalog 单独生成不可变 60 规则计划，不查看历史评测样本或结果。
- 精确规则配额为 Mechanics 11、Electromagnetism 11、Thermodynamics/Statistical Physics 10、Optics 10、Modern Physics 10、Experimental Physics 8；`gen`/`exp` 各 30、词法 `broad_proxy`/`narrow_proxy` 各 30、无/有 symbolic primitive 为 14/46。
- `trigger_scope_proxy` 由 trigger 的 NFC/空白规范化文本在每个 `Domain × origin` 内按 Unicode 字符长度二分。该字段只是可复现的词法 proxy，不是语义宽窄标签。
- `gemini-3-flash-preview` 对每条规则构造 5 类机制样本，共 300 例；严格 GT 校验 exact key、固定 expected 映射、证据来源、唯一 quote 和字符 span。失败规则不可被其他规则替换。generation manifest 绑定 catalog、计划、模型/Prompt、源码/conda/API 身份、dataset、raw trace、checkpoint 和 target trace。
- 主实验使用 honest `frozen_target_binding`，每个样本只提供其预注册目标规则；`fixed_control_0_1=1.0` 是控制绑定常量，不是 semantic 分数。故主结论仅覆盖条件 Checker + release gate，不覆盖 retrieval 或端到端 verifier。
- 三个 arm 为 `legacy`、`dual_evidence`、`dual_evidence_consistency`，各独立重复 3 次。前两者是消融 baseline；只有 `dual_evidence_consistency` 参与 acceptance。所有格子关闭 cache，使用 Qwen30B 和不同 output/report/raw trace。
- 评估器分别构造 `C[r]`（重复内三臂共同有效）、`T[a]`（臂内三重复共同有效）和 `G`（九格全局共同有效）。API/解析/schema/缺失/绑定审计失败只降低 coverage，不能当作 TN/FN。
- `run_verifier.py --retrieval-only` 兼容 trace 可独立报告目标可执行命中、被抑制、仅错误规则、空结果、retrieval failure、排名和候选数；当前 provenance 为未验证，且不与主门禁合并。
- 最终协议 Gemini development smoke 的公共审计为 `valid=true`、1/1 规则、5/5 样本、无 orphan，请求模型与实际响应模型均为 `gemini-3-flash-preview`。同一冻结产物上的 Qwen30B 三臂各一次身份回放均 5/5 完成、无 Checker failure；15 个响应的请求模型和供应商 `actual_model` 均精确为 `qwen3-30b-a3b-instruct-2507`，15 个 `response_id` 全部非空且唯一。严格 evaluator 的全局共同有效集为 5/5，`formal_gate` 未判定。
- 该 5 例 smoke 中，`dual_evidence` 与 `dual_evidence_consistency` 都在唯一 self-corrected 例上发布了诊断：模型把已明确纠正的早期错误判成 `confirmed_violation`，证据 span 与 GT 的 superseded span 重叠。此现象只说明预注册 self-corrected probe 确有必要；未据此改 prompt、规则或代码，必须由正式 30 个 self-corrected 样本估计普遍性。
- 正式 60 规则/300 例 Gemini 生成与 2700 个 Qwen 运行单元均未执行，暂无效果结论。

## 规则库深度审计结论

### 1. 1123 库结构有效，但语义发布证据不足

- 6 个 Domain、123 个 Topic 中有 76 个 Topic 含规则；189 个 Cluster 中有 3 个空 Cluster，均位于没有规则的 `Optics / Laser Principles and Applications`。
- Topic 规则数中位数为 9，最大为 78；Cluster 规则数中位数为 5，最大为 29。粗化阶段所称“最大 12”只针对 102 个 residual bucket，不代表所有 Cluster。
- 609 条 generalized 规则在构建审计中均有至少两个样本支持；514 条 baseline 规则仍是单样本历史豁免。精简运行库没有携带 support/provenance 和 runtime metadata，运行时无法据此审计前提或来源。
- 真正进入 embedding 主簇的规则只有 343/1123；572 条仍位于 102 个 deferred residual bucket。结构上的全部可达不等于检索机会均等。
- 当前 semantic publish gate 的默认分数阈值为 0，且不验证 precondition、violation signature 或 evidence requirement。下游 release gate 主要检查 quote 可定位性和关键词/符号重合；这些条件不能证明物理违规。

### 2. 正确题误报是系统性门控问题

- 原始 1123 的 16 个正确题 FP 共 22 条诊断，涉及 19 条不同规则，不存在可通过黑名单解决的单一坏规则。
- metadata 组剩余 17 条 FP 均有 grounding，并通过了下游关键词/符号重合检查，但 Rule 发布门的题目侧前提和答案侧违规证据仍为空。通用符号 `r`、`h`、`x`、`hbar` 等造成的弱重合不是违规证明。
- 至少 3 条 FP 的解释明确承认所引推导正确或有效，随后仍发布错误，表明还缺少发布前自一致性检查。
- 结论：下一步必须改输出 schema 和确定性发布条件，而不是针对 validation 样本修改规则或继续补 Prompt。

### 3. 1139 增量候选未通过治理审计

- 8 条新错解产生 9 条候选和 16 条新增规则，但只有 1 条新增规则含任一新样本来源；其余 15 条主要由旧样本重新概括产生。
- 与 1123 基线比较，有 118 条旧规则更换 Scenario Cluster，涉及 11 个 Topic，其中 6 个不在声明的 affected topics。
- 历史 `incremental_validation.json` 只比较每个 Topic 的 Rule-ID 集合，没有比较 Cluster 归属，因此错误报告 `unexpected_changed_topics=[]`。
- 历史 manifest 记录的基线 SHA-256 为 `0a299a…`，当前 1123 基线为 `838506…`，原始基线文件已不存在；该候选同时属于 stale、不可复现产物。
- P1 受控回放使用当前 1123 基线重建后，16 条新增 Rule 的蓝图覆盖审计通过，旧规则 Cluster/Rule Group 迁移均为 0、unexpected Topic 为 0，但仍被五类门禁拒绝：新增正式规则 16 超过预注册预算 9，generalized 新增 16、删除 20、修改 12，新增规则本轮来源覆盖仅 1/16。
- 因此 1139 只能保留为审计产物；结构漂移被消除不等于来源和概括变化合格。

完整规则库结构、分布和维护边界见[规则库说明](规则库说明.md)。

## 唯一主线任务

在不修改 1123 catalog 或根据测试样本添加特例的前提下，完成 P3 正式 300 例生成、三臂三重复条件 Checker 门禁，并为 semantic retrieval 增加执行 sidecar 后再做独立外部效度审计。已落地的通用不变量为：

1. `applicability`：只能引用 `question/context`，证明规则前提在题目中成立；
2. `violation`：只能引用 `prediction`，证明答案中的具体陈述违反该规则；
3. 两段证据都必须通过严格 source/span 校验，`dual_evidence_consistency` 还必须确认当前结论确有违规；否则不发布诊断；
4. metadata 只参与检索排序和证据提示，不单独作为硬拒绝条件。

## 下一步执行计划

### P0：冻结身份与复现条件（已完成）

- 已实现 `schema_version=1` 实验 manifest，自动记录 Git commit/dirty/diff、逐文件源码、dataset/catalog/output SHA-256、Prompt 版本、模型、参数、命令、conda 和失败阶段。
- `run_physics_eval_pipeline.py` 已强制 conda 与显式 `unified_rules_v2` catalog；validation/final 拒绝 dirty worktree。
- 受控重复默认关闭跨运行 LLM cache；完整 verifier 每题原子 checkpoint，语义失败保留退出码和 trace 后继续计算 coverage。
- 冻结清单进入 `experiments/manifests/`；`data/` 与 `results/` 使用非 Git 通道同步并按 SHA 校验。
- legacy 移动按旧路径删除与新路径新增成对收口，不再把删除和归档拆开处理。

### P1：做无语义损失的结构修复（已完成）

- lexical 与 semantic runtime 均已排除零规则 Topic、空 Cluster 和失效规则引用；原始 catalog 仍为 123 Topic / 189 Cluster，实际候选收敛为 76 Topic / 186 Cluster，未删除 catalog 节点或可执行规则。
- 增量 manifest 升级为 v2，冻结 8 类外部基线输入和 3 个确定性候选准备产物；六阶段产物以 configuration SHA 和直接父文件 SHA-256 串成完整依赖链，finalize 在构建前重算 candidate delta，并确定性重放 formal/precluster。
- 增量验证已覆盖 generalization/cluster-label 完整性、Domain/Topic 导航定义、Domain→Topic→Rule/Cluster→Rule Group→Rule 各层旧节点相对顺序、旧规则内容、Rule→Cluster→Rule Group、Cluster 定义、unexpected Topic/Cluster、generalized 增删改、候选来源和预注册预算。
- additive 模式从基线 catalog 恢复权威 Cluster 拓扑，旧规则和顺序保持不变；scoped 重聚类必须在 prepare 阶段显式声明 Topic 与预算，并对 Topic 全部正式 Rule 做 exact-once 蓝图覆盖校验。
- generalization 和 cluster-label 的缓存指纹绑定输入内容、Prompt 版本、模型链和行为参数，产物身份另行绑定本轮配置与父 SHA；scope/预算变化不再导致无关模型缓存失效，旧的无指纹结果默认不复用。
- cluster-label 的 `embedding_fallback` 只是网络/API 中断时的可恢复 checkpoint，不具备晋升资格；resume 会自动重试，只有全部目标 Topic 均为 `label_source=model` 才能通过 finalize。

### P2：双证据门控（工程收口已完成）

- 三模式、双证据 source/span 门、固定 `0.8` 置信门、自一致性门、Checker failure coverage 口径和非 `legacy` bottom-up 审计隔离已实现。
- 冻结 retrieval 的回放入口已实现；运行时必须先生成不可变 manifest，再对同一 retrieval trace 分别运行三个单模式 system arm，最后冻结共同有效配对集。
- 回放 report 绑定 source/runtime/API/frozen-manifest 身份，正式 run-kind 禁止 dirty worktree 和 Checker cache；resume 对 sidecar、trace 前缀及 orphan trace 做 fail-closed 审计。
- P2 专项与全量回归已经通过；本轮合并 P3 与响应身份门禁后全量回归为 368/368 通过。既有两题机制 smoke 中，`dual_evidence` 对明确矛盾发布 1 条诊断，`dual_evidence_consistency` 对已自我修正陈述返回合法空结果且发布 0 条诊断，两者均无传输、解析或 schema failure。
- 上述 smoke 不构成效果判断。下一步按已实现的 P3 协议生成正式独立机制样本并运行冻结回放，不回看历史 5+5、20+20 结果做修改。
- 不增加样本 ID、Rule ID、题型或公式特判。

### P3：规则级合成门禁与独立 development（工程完成，正式运行待执行）

- 固定抽样不再使用“至少”：严格为 60 条规则、300 例。Domain 配额为 11/11/10/10/10/8，origin 为 30/30，词法 trigger proxy 为 30/30，有/无 symbolic primitive 为 46/14；分层不足立即失败，不回退、不替补。
- GT 模型固定为 `gemini-3-flash-preview`，Verifier 固定为 `qwen3-30b-a3b-instruct-2507`。每条规则生成真实违规、适用但正确、仅符号重合且不适用、等价/替代、信息不足或自我修正五类样本。标签、证据和 generation manifest 必须在 Qwen 运行前冻结。
- 主实验使用 explicit target binding，回答条件 Checker + release gate 问题。三臂 × 三重复组成九格；`legacy` 与 `dual_evidence` 为 baseline，`dual_evidence_consistency` 为唯一候选臂。
- 候选臂每次重复都必须满足：结构化输出率 ≥ 99%、真实违规 Recall ≥ 90%、四类负机制 FPR 分别 ≤ 5%、self-corrected 诊断数为 0、可评估 protocol contradiction 数为 0。三次发布一致率必须 ≥ 95%；不允许按三次平均或合并负类后过线。
- `C[r]`、`T[a]`、`G` 分别用于三臂配对、重复稳定性和全局审计；失败只降 coverage。secondary semantic trace 不进入主门禁 acceptance；未有 sidecar 时只报告 schema-compatible 分类。
- 主门禁通过后，才另建 30 道错题 + 30 道正确题的端到端 development 集，每个候选重复 3 次，并保留 direct baseline。所有规则构造样本、5+5 pilot 和 20+20 validation 都按归一化题目文本排除。
- metadata 排序若后续加入，必须另立预注册实验，不混入当前三臂。

候选必须同时满足以下预注册门槛：

- 合成集门槛按上文逐重复、逐负机制执行，不汇总稀释；
- 30+30 development 每次错题 Recall ≥ 90%、正确题 Specificity ≥ 70%；
- 相对原始 1123，错误定位 Recall 下降不超过 5 个百分点，错误定位 Precision 提高至少 10 个百分点；
- 三次题目级决策一致率 ≥ 90%，semantic/parse failure 必须单列。

任一门槛失败即否决，不用 20+20 validation 调参补救。

### P4：冻结、全新 validation 与 final test

- 候选通过 development 后初步冻结代码、Prompt、catalog、模型与参数。旧 20+20 只作历史回归，不参与选择或调参。
- 抽取全新 50 错 + 50 对 validation，执行一次 go/no-go：Coverage ≥ 98%、Recall ≥ 90%、Specificity ≥ 60%、题目级 F1 不低于同批 direct baseline、定位 F1 至少高 5 个百分点、自相矛盾诊断为 0。任一失败即否决；若据此修改，整套 validation 降级为 development 并重新抽取。
- 通过后再抽取全新 final test，建议 100 道错题 + 100 道经 Gemini 审计的正确题；在服务器 `tmux` 中运行冻结的胜出方案与 direct baseline。
- 报告题目级、错误定位级、coverage、失败阶段、耗时和配对置信区间；final test 若触发修改，整套样本作废并重抽。

## 当前明确不做

- 不扩大规则数量，不晋升 1139，不采用 4875 或现有 metadata 硬门控。
- 不用已经查看的 validation 样本编写规则、Prompt 特例或阈值。
- 不把 semantic/parse failure 计为 TN，不把结构质量分当作端到端效果分。
- 不在 precision gate 通过预注册门槛前启动服务器 final test。
