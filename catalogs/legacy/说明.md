# 历史文件说明

本目录保存旧结构、中间产物和实验版本，不用于当前 verifier 主流程。

- `rules_hierarchy.json`：旧层级结构。
- `semantic_experience_300.json`：早期经验规则中间产物。
- `unified_rule_library_v2_*.json`：514 条基线规则的不同实验版本。

需要复现实验时应显式指定本目录中的文件，不能将其替换为当前运行库。
