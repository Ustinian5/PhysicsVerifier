[project]
name = "physicsverifier"
version = "0.1.0"
description = "Evaluate physics-competition solutions with semantic rule retrieval and optional symbolic checks."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "antlr4-python3-runtime==4.11.1",
    "sympy>=1.14.0",
]
